### frontDesk:前台所有接口

#### 1:获取所有的客户信息

**URL**:http://localhost:8080/frontDesk/getAllClient

**传入参数列表**：

| 参数名 | 描述                          |
| ------ | ----------------------------- |
| keyword  | 查询的关键字,可以是任意属性的关键字除开密码 若不传则全部查询|
| page     | 查询的页数(不传默认为1)  |
| pageSize | 查询的页数大小(默认为20) |

**返回参数列表**:

| 参数名  | 描述                            |
| ------- | ------------------------------- |
| success | 此次请求是否成功(true Or false) |
| code    | 此次请求的结果                  |
| message | 此次请求的信息|
| clientList |查询到的客户列表|

#### 2:获取所有的员工信息

**URL**:http://localhost:8080/frontDesk/getAllStaff

**传入参数列表**：

| 参数名 | 描述                          |
| ------ | ----------------------------- |
| keyword  | 查询的关键字,可以是任意属性的关键字除开密码 若不传则全部查询|
| page     | 查询的页数(不传默认为1)  |
| pageSize | 查询的页数大小(默认为20) |

**返回参数列表**:

| 参数名  | 描述                            |
| ------- | ------------------------------- |
| success | 此次请求是否成功(true Or false) |
| code    | 此次请求的结果                  |
| message | 此次请求的信息|
| staffList |查询到的员工列表|
#### 3:前台修改客户的个人信息

**URL**:http://localhost:8080/frontDesk/updateClientInfo

**传入参数列表**：

| 参数名 | 描述                               |
| ------ | ---------------------------------- |
| client | 对应的客户实体(客户Id一定不能为空) |

**返回参数列表**:

| 参数名  | 描述                            |
| ------- | ------------------------------- |
| success | 此次请求是否成功(true Or false) |
| code    | 此次请求的结果                  |
| message | 此次请求的信息                  |
#### 4:前台修改员工的个人信息

**URL**:http://localhost:8080/frontDesk/updateStaffInfo

**传入参数列表**：

| 参数名 | 描述                               |
| ------ | ---------------------------------- |
| client | 对应的客户实体(客户Id一定不能为空) |

**返回参数列表**:

| 参数名  | 描述                            |
| ------- | ------------------------------- |
| success | 此次请求是否成功(true Or false) |
| code    | 此次请求的结果                  |
| message | 此次请求的信息                  |
#### 5:前台增加员工

**URL**:http://localhost:8080/frontDesk/addStaff

**传入参数列表**：

| 参数名 | 描述                               |
| ------ | ---------------------------------- |
| staff | 对应的客户实体(客户Id一定不能为空) |

**返回参数列表**:

| 参数名  | 描述                            |
| ------- | ------------------------------- |
| success | 此次请求是否成功(true Or false) |
| code    | 此次请求的结果                  |
| message | 此次请求的信息                  |

#### 6:前台修增加客户

**URL**:http://localhost:8080/frontDesk/addClient

**传入参数列表**：

| 参数名 | 描述                               |
| ------ | ---------------------------------- |
| client | 对应的客户实体(客户Id一定不能为空) |

**返回参数列表**:

| 参数名  | 描述                            |
| ------- | ------------------------------- |
| success | 此次请求是否成功(true Or false) |
| code    | 此次请求的结果                  |
| message | 此次请求的信息                  |
