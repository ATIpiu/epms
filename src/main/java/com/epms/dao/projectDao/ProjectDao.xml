<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.epms.dao.projectDao.ProjectDao">

    <insert id="insertIntoProject" parameterType="com.epms.entity.Project"
            useGeneratedKeys="true" keyProperty="pId" keyColumn="pId">
        INSERT INTO
		project(pId, pName, pCreateDate, pEndDate, pType, cId, pModelManagerId, pModelStaff,
         pRenderManagerId, pRenderStaff, pAfterManagerId, pAfterStaff, pPeriodStatus, pProjectStatus,
         pFileUrl, pModelWage, pRenderWage, pAfterWage)
		VALUES
		(#{pId},#{pName},#{pCreateDate},#{pEndDate},#{pType},#{cId},#{pModelManagerId},#{pModelStaff},
		#{pRenderManagerId},#{pRenderStaff},#{pAfterManagerId},#{pAfterStaff},#{pPeriodStatus},#{pProjectStatus},
		#{pFileUrl},#{pModelWage},#{pRenderWage},#{pAfterWage})
    </insert>

    <delete id="deleteProjectBypId" >
        DELETE FROM project WHERE pId=#{pId}
    </delete>

    <update id="updateProject" parameterType="com.epms.entity.Project">
        UPDATE project
        SET pName=#{pName},pCreateDate=#{pCreateDate},pEndDate=#{pEndDate},pType=#{pType},
        cId=#{cId},pModelManagerId=#{pModelManagerId},pModelStaff=#{pModelStaff},
        pRenderManagerId=#{pRenderManagerId},pRenderStaff=#{pRenderStaff},
        pAfterManagerId=#{pAfterManagerId},pAfterStaff=#{pAfterStaff},
        pPeriodStatus=#{pPeriodStatus},pProjectStatus=#{pProjectStatus},pFileUrl=#{pFileUrl},
        pModelWage=#{pModelWage},pRenderWage=#{pRenderWage},pAfterWage=#{pAfterWage}
        WHERE pId=#{pId}
    </update>


    <select id="queryProjectBypId" resultType="com.epms.entity.Project">
        SELECT pid, pname, pcreatedate, penddate, pType, cid, pmodelmanagerid,
         pmodelstaff, prendermanagerid, prenderstaff, paftermanagerid, pafterstaff,
         pperiodstatus, pprojectstatus, pfileurl, pmodelwage, prenderwage, pafterwage
         FROM project WHERE pId=#{pId}
    </select>

    <select id="queryProjectBycId"  resultType="com.epms.entity.Project">
        SELECT pid, pname, pcreatedate, penddate, pType, cid, pmodelmanagerid,
         pmodelstaff, prendermanagerid, prenderstaff, paftermanagerid, pafterstaff,
         pperiodstatus, pprojectstatus, pfileurl, pmodelwage, prenderwage, pafterwage
         FROM project WHERE cId=#{cId}
    </select>

    <select id="queryAllProjects" resultType="com.epms.entity.Project">
        SELECT  pid, pname, pcreatedate, penddate, pType, cid, pmodelmanagerid,
         pmodelstaff, prendermanagerid, prenderstaff, paftermanagerid, pafterstaff,
         pperiodstatus, pprojectstatus, pfileurl, pmodelwage, prenderwage, pafterwage
         FROM project
    </select>
</mapper>